spring:
  application:
    name: api-gateway



  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: https://keycloak-deploy-st9e.onrender.com/realms/fitness-oauth2/protocol/openid-connect/certs

  cloud:
    loadbalancer:
      enabled: false

    gateway:
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials RETAIN_FIRST
        - TokenRelay=

      httpclient:
        connect-timeout: 10000
        response-timeout: 60s

      # ✅ CORS Configuration Added
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins:
              - "https://fitness-frontend-6ogz.onrender.com"  # ✅ Replace with your frontend URL
              - "http://localhost:5173"
              - "http://localhost:3000"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
              - PATCH
            allowedHeaders:
              - "*"
            allowCredentials: true
            maxAge: 3600

      routes:
        - id: userservice
          uri: http://userservice-n20l.onrender.com:8081 # Changed https to http
          predicates:
            - Path=/api/users/**
          filters:
            - StripPrefix=1

        - id: activityservice
          uri: http://activityservice-pcce.onrender.com:8082 # Changed https to http
          predicates:
            - Path=/api/activities/**


        - id: aiservice
          # Recommendation: Use internal URL here too if possible
          uri: http://aiservice-z1dw:8083
          predicates:
            - Path=/api/recommendations/**
          filters:
            - StripPrefix=1

server:
  port: ${PORT:8080}

eureka:
  client:
    enabled: false

# ✅ Enable actuator to debug routes
management:
  endpoints:
    web:
      exposure:
        include: health, gateway, info
  endpoint:
    gateway:
      enabled: true

logging:
  level:
    org.springframework.cloud.gateway: DEBUG   # ✅ See routing decisions
    org.springframework.security: DEBUG        # ✅ See auth decisions
    reactor.netty: DEBUG